export default function PrivacyPage() {
  return (
    <main className="content" style={{ margin: "0 auto" }}>
      <div className="card">
        <h1>Privacy</h1>
        <p className="muted">
          Website URLs, audit reports, notifications, and share links are private by default. Shared reports expose only
          the selected audit report and never expose account settings or unrelated websites.
        </p>
      </div>
    </main>
  );
}
